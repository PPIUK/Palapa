import { ReactElement } from 'react';
import { TChildrenRendererProps } from './shared-types';
export default function renderChildren({ tchildren, propsForChildren, disableMarginCollapsing, renderChild }: TChildrenRendererProps): ReactElement;
